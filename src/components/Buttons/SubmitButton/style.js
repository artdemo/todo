import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  progress: {
    display: 'block',
    margin: 'auto',
  },
  button: {
    color: 'green',
    height: '100%',
  },
});

export default useStyles;
