import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  form: {
    marginBotton: 12,
  },
  progress: {
    display: 'block',
    margin: 'auto',
  },
  button: {
    color: 'green',
    height: '100%',
  },
});

export default useStyles;
