import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  form: {
    marginBottom: 12,
  },

  formControl: {
    width: '100%',
  },

  progress: {
    display: 'block',
    margin: 'auto',
  },

  button: {
    color: 'green',
    height: '100%',
  },

  paper: {
    padding: 10,
  },

  gutters: {
    padding: 0,
  },
});

export default useStyles;
